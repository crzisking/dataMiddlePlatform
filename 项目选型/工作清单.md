# 工作清单 / 实施路线图

> 本文档把整个中台的工作按阶段排序，并标明每个**待讨论事项**应在哪个阶段**之前 / 之后**展开。
> 选型详情见 [README.md](README.md)、[中台架构.md](中台架构.md)、[RAG系统.md](RAG系统.md)、[TextToSQL.md](TextToSQL.md)。

图例：✅ 已完成　🔨 待开发　💬 待讨论（含触发时点）

---

## 进度快照（2026-06-26）

- **🚀 上线部署进行中（2026-06-26）**：后端已离线部署到**测试服务器 `192.168.120.174`(CSVSPM901)**并跑通——API + Worker 运行中、外部可访问、PG/SQLServer/通义 全连通、检索实测可用。详细步骤与踩坑见 [运维手册.md](运维手册.md) 第十三节「离线部署实录」。**关键坑**：老 CPU(Core2Duo,无 x86-64-v2)→ numpy 降 2.2.6；内网隔离 → 离线 wheel 包装(`uv pip install --no-index --find-links wheels`)；uvicorn 默认 Proactor → 用 `run_prod.py`(强制 Selector);Worker 用 `python -m procrastinate`。**待办**：保活(NSSM/任务计划,未做)、前端 build+部署、APP_DEBUG=false 重启确认。**注**:测试机 CPU 极弱,正式生产需换服务器。

## 进度快照（2026-06-25）

- **已完成 P0–P3 + P4(RAG问答)实质完成**：上传→入库→混合检索(向量+BM25+rerank)→Agent问答→多轮记忆→引用来源(带MinIO下载链接) 全链路通。
- **P4 来源修复(2026-06-25)**：多轮里"模型吃历史不再检索→sources空"已修——A 每轮强制重检索、B 来源随消息存库(messages.sources)、C `conversation_id` 不传则后端新建返回(去掉共享 "default")。
- **P5 TextToSQL：代码侧全就绪、视图接入延期(2026-06-25)**：A1 连接、B1 语义层、B2 拉字段脚手架、B4 检索、C1 生成、D1/D2 护栏执行、E1 接 Agent 全部 ✅；操作手册见 [TextToSQL.md](TextToSQL.md) 第八节。**A2/B3(建视图+补含义)延期**，视图备好即可接入。**ICAS_FPC 是生产库，绝不碰**。
- **P4-④ 工具进度流式：决定不做**（明确放弃该增强）。
- **P6 Agent 编排调优（部分完成，2026-06-25）**：系统提示词 + 两个工具 docstring 打磨（清晰选工具规则 + 必调工具/查不到不编造）✅；工具失败兜底 ✅；实测 4 类问题全部选对工具、无编造。**迭代检索(agentic RAG)✅**：提示词要求"结果不全就换关键词再查"，实测同一问题检索次数 1→4，不再拿不全结果硬答。混合型多步验证待 P5 视图上线。
- **P7 前端管理页 起步（2026-06-26）**：前端仓库 `D:\workSpace\tmbomweb`(Vue3+Vite+ElementPlus)。新增数据中台地址 `VITE_AI_API`(开发=本机:8000、正式预留) + 专用 axios `requestAI`(无鉴权/裸JSON)；建了 **5 个管理页**：文档管理、切割配置、检索测试、智能问答(流式+非流式)、会话历史。详见 P7 明细。
- **配套后端增强（为管理页，2026-06-26）**：① `GET /meta/doc-types` + `DOC_TYPES` 受控词表(上传/筛选下拉来源)；② `GET /documents` 加 `name`(文件名模糊)+`created_from/to`(时间范围)过滤 + `GET /documents/{id}/download-url`(现签下载)；③ 修复 uvicorn --reload 监视 logs 自我刷屏。
- **P8 上线前硬化（决策已定，2026-06-26，开工在即）**：CORS 不收敛、鉴权不做（内网）；C 全做(重试/连接池/兜底) + D1 请求日志；成本控制(B3)延期随 i18n；A3关DEBUG/B1限流/B2上游容错待二次确认；D2告警/D3 Langfuse 待讨论。正式部署 `192.168.20.99`。
- **模型白名单改 .env 配置**：`QWEN_MODELS` / `DEEPSEEK_MODELS`，当前 `qwen3.7-max` / `qwen3.7-plus` / `deepseek-v4-pro`（默认 `qwen3.7-plus`）。
- **已灌入 63 篇真实文档**（来自 `C:\Users\Admin\Desktop\ragUse`，.doc/.wps 经 Word 转 docx 后入库，doc_type=通用；共 642 chunks）。
- **运维就绪**：`scripts/ops.ps1`(api/worker/migrate/ingest/health/mssqlcheck/scaffold)、`batch_ingest.py`、`check_mssql.py`、`register_views.py`、`scaffold_schema.py`、[运维手册.md](运维手册.md)；**应用日志落文件** logs/api.log、logs/worker.log(轮转)。
- **评测**：见 [评测问题集.md](评测问题集.md)。文字型问答准确带来源；图片型 11 篇覆盖薄、需 OCR；模型会"加戏"补文档外常识(已加严提示词收敛)。

---

## 阶段总览（先后顺序）

```
P0 选型              ✅ 已完成
   │
P1 后端骨架           ✅ 已完成
   │
P2 RAG 入库链路        ✅ 已完成
   ├─ ① 数据模型 + 迁移        ✅
   ├─ ② 上传限制 + 校验        ✅
   ├─ ③ 上传接口(存MinIO+登记+投递) ✅
   ├─ ④ 异步入库任务(解析→切割→embedding→写chunks→状态)  ✅
   └─ ⑤ 回查 / 列表接口        ✅
   │
P3 检索 + 切割优化         ✅ 已完成
   ├─ ① 向量检索(query→向量→PGVector 近邻)  ✅
   ├─ ② 中文 BM25(jieba 分词 + PG 全文检索)   ✅
   ├─ ③ 混合检索(向量 + BM25 + rerank 可选)   ✅
   └─ ④ 切割策略可配置(配置表 + 父子切割)      ✅（标题/表格策略待解析升级）
   │
P4 RAG 问答              ✅ 实质完成
   ├─ ① 接通 RAG 工具(search_knowledge_base→混合检索)  ✅ 端到端问答已通
   ├─ ② 会话存储(会话表+消息表，按 conversation_id 存取历史)  ✅ 多轮记忆已通
   ├─ ③ 引用来源(答案带 sources + MinIO 下载链接)  ✅
   │     └─ 修复(2026-06-25)：多轮里模型"吃历史不再检索→sources空"的坑。
   │        A 系统提示词强制知识类问题每轮重新检索；B 来源随消息存库(messages.sources)、
   │        历史回看现签链接；C 去掉共享的 "default" 会话默认值——conversation_id 不传则
   │        服务端生成新 ID 并返回(开新对话)、传旧 ID 接着聊，杜绝历史串台(stream 加 meta 事件回传 ID)。
   │        三者均验证通过。详见 chat.py。
   └─ ④ 工具进度流式("正在检索…")  ❌ 不做（2026-06-25 决定：明确放弃）
   │  └─（RAG 效果评测：见 评测问题集.md）
P5 TextToSQL          🔨 代码链路全就绪；A2/B3 视图接入延期(DBA)
   │     A1连接 B1语义层 B2拉字段 B4检索 C1生成 D1/D2护栏执行 E1接Agent 全✅
   │  └─（P5 跑通后 💬 讨论「TextToSQL 评测」F1）
P6 Agent 编排调优（工具协同）🔨 提示词/工具说明/失败兜底/迭代检索✅；混合型多步验证待P5视图
   │
P7 前端管理页 / 上传页  🔨 进行中 ← 我们在这（仓库 D:\workSpace\tmbomweb）
   │     5 页已建：文档管理 / 切割配置 / 检索测试 / 智能问答(流式) / 会话历史
   │
P8 上线前硬化          🔨 本轮完成：A3关DEBUG/B1限流400/B2上游容错/C全做/D1请求日志；D2邮件告警+B3成本延期，Langfuse不上
   │
P9 压测 → Docker 迁移   ⬜
```

---

## 各阶段明细

### P0 · 选型 ✅（已完成）
- 技术栈、架构、各项约束已定稿（见四份选型文档）。

### P1 · 后端骨架 ✅（已完成）
- 项目目录结构、FastAPI 启动、配置管理（环境变量/配置文件）
- PostgreSQL 连接 + PGVector 扩展启用 + Alembic 迁移
- MinIO 连接封装
- LLM 接入层（通义/DeepSeek，OpenAI 兼容，按模型标识路由）
- **多轮对话 Agent 骨架**（LangGraph 工具调用 Agent + 工具注册表；工具为占位）
  - Agent 无状态（方案 B）：历史由调用方以消息列表传入
  - 对话接口：`/chat`（一次性）+ `/chat/stream`（SSE 流式，已打通）
- procrastinate（PostgreSQL 任务队列）骨架空壳
- 统一异常处理、冒烟测试、Windows 事件循环修复（SelectorEventLoop）

### P2 · RAG 入库链路 ✅（已完成）
**① 数据模型 + 迁移 ✅**
- `documents` / `document_chunks` 两表建好，含 HNSW 向量索引、外键级联
- 流程沉淀为 [数据库与迁移SOP.md](数据库与迁移SOP.md)

**② 上传限制 + 校验 ✅**
- 白名单 PDF/Word/Excel/txt/md（后缀 + 魔数双校验、挡可执行/伪装文件）
- 单文件 100MB；同名同类型自动新版（详见 [RAG系统.md](RAG系统.md)）
- 配置可经 `.env` 调整；9 个单元测试

**③ 上传接口 ✅**
- `POST /api/v1/documents`：校验 → 存 MinIO → 建文档记录(pending) → 投递异步任务
- 端到端验证通过（DB + MinIO + 队列三处确认）

**④ 异步入库任务 ✅**
- worker 取任务后：从 MinIO 取原件 → 解析（PyMuPDF / python-docx / openpyxl / txt·md）
  → 递归切割 → 通义 embedding(1024 维) → 写 `document_chunks` → 更新 `documents.status`
- 失败落 `status=failed` + `error`；扫描件无文字层会 done+0 块（OCR 留作后续增强）
- 直连测试 + worker 集成测试均通过（真实通义 embedding）
- 老格式 .doc/.xls 暂不支持解析（明确报错引导转存 docx/xlsx）

**⑤ 回查 / 列表接口 ✅**
- `GET /api/v1/documents/{id}`：单篇详情/状态（供上传后轮询进度）
- `GET /api/v1/documents`：分页列表，支持按 doc_type / status / only_active 过滤
- 真实 docx（MRP整理.docx）端到端验证通过（解析/切块/向量/biz_tags 元数据均正确）

### P3 · 检索 + 切割优化 ✅（已完成）

**① 向量检索 ✅**
- `app/services/rag/retrieval.py`：query → 通义向量 → PGVector 余弦近邻 → Top-K 块
- 支持按 doc_type 过滤、只搜有效版本；`POST /api/v1/search` 测试接口
- 用真实 MRP 文档实测召回正常

**② 中文 BM25 ✅**
- jieba 分词(`tokenize.py`)：入库写 `content_tokens`；`ts` 生成列 + GIN 索引(迁移 eebf3ca13bb4)
- `retrieval.keyword_search()`；search 接口加 `mode=vector|keyword`
- 实测验证互补：精确词(PAB)关键词能命中、语义词(采购下单)向量能命中

**③ 混合检索 ✅**
- `hybrid_search()`：向量 + 关键词两路用 RRF(倒数排名融合)合并
- rerank 可配开关(`RERANK_ENABLED`)：通义 gte-rerank-v2(DashScope HTTP)精排，已实测调通
- search 接口 `mode=hybrid|vector|keyword`，默认 hybrid

**④ 切割策略可配置 ✅**
- `chunk_configs` 表 + 管理接口(`GET /chunk-configs`、`PUT /chunk-configs/{doc_type}`)：每类文档配 策略/chunk大小/重叠/父块大小
- 切割策略路由：`recursive`(默认) + `parent_child`(small-to-big：小块检索、命中返回所在父块)
- ingest 按文档类型读配置切割；检索时父子切割返回父块(实测验证)
- ⚠️ **标题层级/表格独立策略未做**：依赖解析保留文档结构(标题/表格)，当前解析拍平成纯文本 → 记入技术债

### P4 · RAG 问答 ✅（实质完成；④工具进度流式 ❌不做）

**① 接通 RAG 工具 ✅**
- `search_knowledge_base` 工具接到 `hybrid_search`：检索 Top-5 → 拼成带来源的文本给模型
- 端到端实测：问"MRP 怎么算采购量" → Agent 自动检索 MRP 文档 → 据此作答(GR/PAB/NR/Pr 逻辑来自文档)

**② 会话存储 ✅**
- `conversations` + `messages` 表(PG，方案 B 唯一真相)；`/chat` 按 conversation_id 取历史→喂 Agent→答完写回
- `persist` 开关：网页端(True)服务端存取；桌面端(False)用请求自带 history、不碰库
- 历史接口：`GET /conversations`(列会话)、`GET /conversations/{id}/messages`(查消息)
- 多轮记忆端到端实测通过(记名字→下一轮答对)

**③ 引用来源 ✅**
- 检索工具用 ContextVar 记录命中的来源文档，接口在回答后整理成 sources
- `/chat` 响应加 `sources`（document_id/name + MinIO 预签名下载链接）；`/chat/stream` 末尾推 `sources` 事件
- 实测：答案带来源、下载链接 HTTP 200 可下原件

**④ 工具进度流式 ⬜（可选增强，可跳过）**
- 流式时推"正在检索…"等工具进度

💬 **本阶段跑通后，讨论「RAG 效果评测」**（有了可跑的链路才好设计评测集与指标）。

### P5 · TextToSQL 🔨

> 架构级决定已拍板（见 [TextToSQL.md](TextToSQL.md)）：只读 SQL Server、无跨库 JOIN、
> TextToSQL/RAG 都做成 Agent 工具、语义层 + schema-RAG、SQL 白名单护栏。
> 本阶段按下面 A–F 六类推进，依赖顺序：**A → B →（C ∥ D）→ E → F**。
> 起步策略（已定）：**单一业务域、几张视图先打通全链路，再复制扩展**；语义层**人工梳理**（库里基本无注释）。
> 详细设计（视图-语义层方案、embedding、大数据量约束、失败点分析）见 [TextToSQL.md](TextToSQL.md) 第七节。

**语义层支持两类数据源**（已定，详见 TextToSQL.md 7.3.2）：
- **① 视图**：DBA 建好的 `v_ai_` 宽视图（已 JOIN、已翻译）—— 首选，准确率高、护栏强。
- **② 文字描述的表**：建不了视图的（如按日期分片的 `xxx_yyyymmdd` 表、过于复杂），只能用文字描述表+字段+关系，模型直接对裸表写 SQL —— 兜底，JOIN/大表风险回归，护栏要更严、需更多样例。

**决策进展**（见 TextToSQL.md 7.3.1）：
- D-a ✅ **`ICAS_FPC` 是生产库 → 绝不碰**：不在里面建任何对象、不跑重查询；只读、必带过滤、NOLOCK。
  - ⚠️ 连锁影响：**生产库里大概率不能建 `v_ai_` 视图**。视图要么建在**单独的报表库/副本库**（待确认有没有），
    要么退到"文字描述裸表 + 只读过滤查询"（7.3.2 ②）。预聚合的汇总表则建在**我们自己的 PG**、低峰期只读拉数，不压生产。
- D-b ⏳ 能否接受预聚合（为高频汇总建定时刷新的汇总表，建在我们 PG）？→ 决定大表聚合走预聚合还是硬扛明细。
- 〔待你确认〕有没有可建视图的**副本/报表库**？没有则第一刀走裸表路径。

**🅰 前置准备**（地基，最先做）
- A1 ✅ SQL Server 连接打通：用 `pymssql`，连通自检 `scripts/check_mssql.py` / `ops.ps1 mssqlcheck` / `GET /health/mssql`。
  - 实测连上 **192.168.20.6 / 库 ICAS_FPC / SQL Server 2008 R2 (SP1)**，共 **3515 张表+视图**。
  - ⚠️ 踩坑：pymssql 新版 FreeTDS 用 TDS 7.1~7.4 跟 2008 做 TLS 握手会失败，**只有 7.0 能连**（已设 `MSSQL_TDS_VERSION=7.0`）。代价：`date`/`datetime2` 被降级成字符串返回（值正确，对只读 TextToSQL 够用）。详见 [TextToSQL.md](TextToSQL.md)。
- A2 ⏸ **延期（2026-06-25 决定）**：第一个业务视图/表的建立暂缓。代码侧链路（B1/B2/B4/C1/D1/D2/E1）已全部就绪，
  视图一旦备好即可接入（流程见 TextToSQL.md 第八节操作手册）。〔解锁需：副本库确认 + DBA 整理视图〕

**🅱 语义层**（P5 核心，准确率全靠它）
- B1 ✅ 语义层 ORM 模型 + 迁移（`SchemaDoc`，表 `schema_docs`）：`source_type`(view/table) 区分两类数据源；
  存中文含义、字段含义、关系、样例、别名，及大表护栏元数据 `required_filter`/`default_window`/`max_window`/`isolation`；
  embedding 可空（先建占位行、内容齐了再向量化）。
  - 配套服务 `app/services/texttosql/semantic_layer.py`：`upsert_schema_doc` / `vectorize` / `build_embed_text`（检索文本）/ `render_ddl_card`（喂模型的 DDL 名片）。
  - **预留入口** `scripts/register_views.py`：带占位符的模板，视图整理好填进去运行即登记+向量化（含防呆，未填会拒绝运行）。**全程不碰生产库**。
- B2 ✅ 脚手架 `scripts/scaffold_schema.py`（`ops.ps1 scaffold <视图名>`）：连库拉某视图/表的字段名+类型 → 生成可粘进 register_views.py 的配置块，只需人工补中文含义。`db.list_columns()` 只读查 `INFORMATION_SCHEMA`（不碰数据、不锁表）。**已建未跑**，用时自己执行（生产库不便碰可对副本库跑）。
- B3 ⏸ **延期（随 A2）**：人工补中文含义/关系/样例 → 入库 + 向量化。两类都支持：视图填①、裸表填②（含显式 JOIN 关系）。〔需你/业务方：补含义〕
- B4 ✅ schema-RAG：`semantic_layer.retrieve_schemas()` 按问题向量检索最相关视图/表（只在 enabled+已向量化的行里找；空则短路）。

**🅲 SQL 生成**
- C1 ✅ 生成服务 `texttosql/generate.py`：问题 + 检出视图的 DDL 名片 + 注入今天日期 → LLM 产出一条 SELECT。

**🅳 安全护栏**（制造业硬要求）
- D1 ✅ SQL 校验器 `texttosql/validate.py`：仅单条 SELECT、禁写/DDL/EXEC、视图白名单、必带过滤列检查、注入 TOP 行数上限。**有单测** `tests/test_texttosql_validate.py`。
- D2 ✅ 执行器 `texttosql/db.run_query()`：只读执行、查询超时（连接级）、fetchmany 二次限行、同步驱动丢线程池。〔**未对生产库实跑**，等有视图再验〕

**🅴 接入 Agent**
- E1 ✅ `query_business_database` 已接真实链路（`texttosql/query.answer_business_question`：检索→生成→校验→执行→格式化）。校验/执行失败返回可读提示+记日志，不暴露 SQL。
  - ⚠️ **现状：语义层为空 → 链路在第①步短路返回"未配置"，不会碰生产库**。登记首个视图后才会真正查询。已验证短路行为。

**🅵 待你做（解锁整条链路）**：① 确认有无可建视图的副本库；② 整理首个视图填进 `register_views.py` 运行；③ 然后用真实问题端到端验一次（首次会真查生产库，注意低峰、确认护栏）。

**🅵 评测收尾**
- F1 💬 **本阶段跑通后，讨论「TextToSQL 评测」**（生成 SQL 正确率怎么测）。〔依赖 E1〕

> 💬 「语义层构建/维护」原为 P5 开工前置门槛 —— 已定方案：人工梳理为主，B2 脚本半自动减负；
> ERP/MES 表结构变更后由维护者重跑 B2 比对、人工更新 B3。初期范围小，维护成本可控。

### P6 · Agent 编排调优 🔨
- 架构已定（多轮工具调用 Agent，骨架在 P1 已建）；本阶段做**多工具协同的调优**：
  - ✅ 工具说明 / 系统提示词打磨（2026-06-25）：重写 `SYSTEM_PROMPT`（清晰的选工具规则 + 硬性要求：必调工具、查不到不编造）+ 两个工具 docstring 给出"何时用/何时不用 + 示例"。实测 4 类问题（知识/数据/闲聊/无答案）全部选对工具、无编造。
  - ✅ 工具调用失败兜底：`search_knowledge_base` 检索异常返回可读提示而非 500；`query_business_database` 经 query.py 内部已兜底（校验/执行失败可读返回）。
  - ✅ **迭代检索（agentic RAG，2026-06-26）**：`SYSTEM_PROMPT` 加"检索后先判断是否完整，不全/不准就换关键词再查（可多次）再作答"。实测同一多方面问题检索次数 1→4（之前只查 1 次就硬答）。机制本就支持多轮（LangGraph 循环），缺的是指示，已补。
  - ⏸ 混合型问题（先查数 + 再检索标准）多步调用验证：需 P5 视图上线、SQL 工具能真返数据后才能完整验（当前 SQL 侧短路）。
- 不再需要写死的"路由判断"——模型自主决定调用工具。

### P7 · 前端管理页 / 上传页 🔨（进行中，2026-06-26）

> 前端仓库：**`D:\workSpace\tmbomweb`**（Vue3 + Vite + TS + Element Plus + Pinia + Router + i18n；门户挂载式，靠 URL `?token=` 拿 JWT）。本仓库是后端，前端是独立仓库。

**接入方式（已落地）**
- 新增数据中台后端地址 `VITE_AI_API`（`.env.dev`=本机 `http://localhost:8000/`、`.env.prd`/`.env.test` 预留空）+ `env.d.ts` 类型。
- 新建专用 axios `src/services/requestAI.ts`：**不带 token（我们后端无鉴权）、适配裸 JSON**（成功直接返回，错误取 `{message/detail}`）；与主后端的 `request.ts` 分开。
- 接口封装 `src/services/DataPlatformService.ts`：文档/切割配置/检索/模型/对话/会话/doc-types + 流式 `chatStream`。

**已建 5 个管理页（`src/views/DataPlatform/`，路由 `/dataPlatform/*`）**
- 文档管理 `DocumentsView`：分页列表 + 类型(下拉)/状态/文件名/时间 过滤 + 多文件上传 + 入库状态轮询。
- 切割配置 `ChunkConfigsView`：按类型配 recursive/parent_child + 参数。
- 检索测试 `SearchTestView`：调检索效果（hybrid/vector/keyword，限类型下拉）。
- 智能问答 `ChatView`：多轮对话 + 模型切换 + **流式/非流式开关** + 来源下载链接 + 新对话。
- 会话历史 `ConversationsView`：列会话 → 看消息（带来源）。

**踩坑修复**
- 流式不输出：sse-starlette 用 `\r\n` 分隔，前端 SSE 解析改为兼容 `\r?\n`。
- 文档类型受控：下拉来自后端 `/meta/doc-types`（`.env` 的 `DOC_TYPES`），前端不写死。

**为前端加的后端能力**
- `GET /meta/doc-types`（文档类型受控词表）；`GET /documents` 加 `name`/`created_from`/`created_to` 过滤；`GET /documents/{id}/download-url`（现签下载链接）。

**体验增强（2026-06-26 已做）**
- 文档下载原件（按钮 + 下载接口）；入库状态自动轮询（有处理中每 3 秒静默刷新、完成即停）；
- 对话流式停止（AbortController）+ 复制；助手回答 Markdown 渲染（`marked`+`dompurify` 消毒，ChatView/会话历史都接）。

**待办**
- 菜单入口已由门户挂好 ✅。
- ⏳ **拉到最后做**：TextToSQL 视图/语义层管理页（需 P5 视图上线 + 后端 schema_docs CRUD）、i18n（当前中文硬串）。
- 不做：按钮权限。
- 本地无 token 调试需动 `oauth.ts`（待定，未做）。

### P8 · 上线前硬化 🔨（决策已定，2026-06-26）

> 一期仅内网，硬化范围按内网实际收敛。**代码质量硬要求**：写好、注释完善（详细解释型），便于长期维护。

**已定决策**
- **CORS 不收敛**：内网跨域无需收敛，维持 `allow_origins=["*"]`（对外公网时再收敛）。
- **鉴权不做**：内网一期维持无鉴权。
- **成本控制（token 上限/记账）延期**：与 i18n 配置国际化一起做（原 B3）。
- **正式环境后端部署在 `192.168.120.174:8000`**（独立机，非 PG 那台）：前端 `.env.prd` 的 `VITE_AI_API` 已指向它。后端需跨网段连 PG(20.99)/MinIO(120.198)/SQL Server(20.6)。

**本阶段已做（2026-06-26）**
- A3 ✅ 关 DEBUG：机制本就由 `APP_DEBUG` 控制（false → 日志 INFO、SQL echo 关）；`.env.example` 注明正式环境设 `APP_DEBUG=false`。
- B1 ✅ 并发限流：`ConcurrencyLimitMiddleware`，同时处理请求数超 `MAX_CONCURRENT_REQUESTS`(默认 **400**) 返回 503；压测后再调。
- B2 ✅ 上游容错：LLM/embedding 客户端设 `max_retries`+`timeout`（openai SDK 对 429/超时自动重试 + 指数退避）。
- C1 ✅ 瞬时失败重试：同 B2（覆盖 embedding 与 Agent 模型调用）。
- C2 ✅ DB 连接池：`pool_size=20`/`max_overflow=20`/`pool_timeout=30` + 连接级 `statement_timeout`(默认 30s) 防慢查询挂住连接。
- C3 ✅ 异常兜底：已有全局处理器；并发 503 走中间件统一结构（{code:"BUSY"}）。
- D1 ✅ 请求日志：`RequestLoggingMiddleware` 每条记 方法/路径/状态/耗时（logger `app.request`）。
- 配置全部走 `.env`（`LLM_MAX_RETRIES`/`LLM_TIMEOUT`/`MAX_CONCURRENT_REQUESTS`/`DB_POOL_SIZE`/`DB_MAX_OVERFLOW`/`DB_STATEMENT_TIMEOUT_MS`）。
- 前端 `.env.prd` 正式地址已填 `http://192.168.20.99:8000/`。
- ✅ **错误分类梳理**：确认 AppError(安全 message)→前端、意外异常→全栈日志+笼统提示；修 `db.py` 一处泄露（SQL Server 原始驱动报错含 IP，改成只进日志、前端只看"业务数据库连接失败"）。app/ 内零 print。
- ✅ **运维手册详细版重写**：12 节，新增 端口数据流 / 配置详解 / **错误分类与日志** / **上线检查清单**。

**延期 / 已决定不做**
- D2 错误告警：**走邮箱，后面写**（延期）。
- D3 Langfuse（LLM 调用追踪）：**不上**——无多余服务器部署（2026-06-26 决定）。靠 D1 请求日志顶着。
- B3 成本控制（token 上限/记账）：延期，随 i18n 一起做。

> P8 本轮(A3/B1/B2/C/D1)已完成；剩 D2(邮件告警,后面写)与 B3(随 i18n)。

### P9 · 压测 → Docker 迁移 🔨
- 按 500~1000 用户 / 200~300 并发压测，验证 Windows 单机瓶颈
- 迁移 Docker（顺带可评估中文分词升级到 zhparser/pg_jieba 方案 B）

---

## 待讨论事项 · 触发时点一览

| 待讨论事项 | 触发时点 |
|---|---|
| 语义层构建/维护 | ✅ 已定（人工梳理 + B2 脚本半自动，见 P5） |
| RAG 效果评测 | 🔨 已起步：见 [评测问题集.md](评测问题集.md)（问题集 + 已测发现），持续补充 |
| TextToSQL 评测 | ⬜ 待 P5 视图上线、链路真跑起来后做（F1） |
| 应用日志 / 监控 | ✅ D1 请求日志已做；D2 邮件告警延期(后面写)；D3 Langfuse 不上(无服务器) |
| 成本控制 / 限流 | 🔨 限流(B1/B2)本轮做；成本控制(B3)延期随 i18n 一起做 |

---

## 技术债（代码审查发现，已记录待办）

> 都不影响当前功能跑通；多数与 P8(上线前硬化)或迁 Docker 阶段契合。

| 事项 | 严重度 | 说明 | 计划 |
|---|---|---|---|
| 上传整文件读内存 | 中 | 100MB × 高并发内存压力 | 改流式直传 MinIO；迁 Docker 前 |
| embedding 无重试/限流 | 中 | 通义偶发错误→整篇入库 failed | 加重试+退避；P3/P4 |
| 缺依赖 DB 的集成测试 | 中 | 上传/入库/列表靠手动脚本验证 | 加测试库 + 集成测试 |
| chunk 元数据无来源信息 | 低 | meta 仅类型/部门，展示来源需回查 documents | 可冗余存 document_id/name；P4 检索时定 |
| 标题层级/表格独立切割未做 | 中 | 依赖解析保留结构(标题/表格)，当前解析拍平成纯文本 | 先升级 parsing(emit markdown 结构)，再加这两种切割策略 |
| 扫描件 OCR 未做 | 中 | 扫描 PDF 无文字层 → 入库 0 块 | 接 MinerU 等 OCR |
| 关键词检索简繁不互通 | 低 | BM25 按字面匹配，简体↔繁体不同字搜不到；繁体 jieba 分词也差(向量路已兜底) | 加 OpenCC 繁→简归一化(入库分词+查询分词)，需重新入库 |

> 已修复（本次审查）：重复 import、错误返回类型、重复入库产生重复切块、
> 切块共享 meta 字典、doc/xls 白名单不一致、版本并发竞态(加唯一约束)、
> 启动强依赖 PG(改为容错启动)。

---

_最后更新：2026-06-24_
